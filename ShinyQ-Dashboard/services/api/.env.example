APP_ENV=development
APP_PORT=8000
CORS_ORIGINS=http://localhost:3000

# Alembic and the API read this. Use localhost when running on your machine; use host `postgres`
# when the API runs inside the same Docker Compose network as the DB.
DATABASE_URL=postgresql+asyncpg://shinyq:shinyq@localhost:5432/shinyq
